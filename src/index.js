import React from 'react';
import ReactDOM from 'react-dom';
import LogEvent from './components/LogEvent'; // Замените на путь к вашему компоненту

ReactDOM.render(<LogEvent />, document.getElementById('root'));
